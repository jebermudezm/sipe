<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jebm.sipe" >

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
 To retrieve OAuth 2.0 tokens or invalidate tokens to disconnect a user. This disconnect
     option is required to comply with the Google+ Sign-In developer policies
    -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" /> <!-- To retrieve the account name (email) as part of sign-in: -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".ClienteActivity"
            android:label="@string/title_activity_cliente"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="jebm.sipe.MainActivity" />
        </activity>
        <activity
            android:name=".ConsClienteActivity"
            android:label="@string/title_activity_cons_cliente"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="jebm.sipe.MainActivity" />
        </activity>
        <activity
            android:name=".ListaProductos"
            android:label="@string/title_activity_lista_productos"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="jebm.sipe.MainActivity" />
        </activity>
        <activity
            android:name=".Pedidos"
            android:label="@string/title_activity_pedidos" >
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateHidden" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuppalActivity"
            android:label="@string/title_activity_menuppal" >
        </activity>
        <activity
            android:name=".MenuSincActivity"
            android:label="@string/title_activity_menu_sinc" >
        </activity>
        <activity
            android:name=".TabActivityClientes"
            android:label="@string/title_activity_tab_activity_clientes" >
        </activity>
        <activity
            android:name=".TabActivityRutas"
            android:label="@string/title_activity_tab_activity_rutas" >
        </activity>
        <activity
            android:name=".Preventa"
            android:label="@string/title_activity_preventa" >
        </activity>

    </application>

</manifest>
